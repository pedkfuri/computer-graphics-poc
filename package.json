{
  "name": "draft",
  "version": "1.0.0",
  "description": "draft projects",
  "main": "./dist/main.js",
  "scripts": {
    "clean": "rm -rf build",
    "start": "npm run build:dev && cd ./build && live-server",
    "build:dev": "npm run clean && npx webpack && cp ./src/index.html ./src/index.css build"
  },
  "author": "pedkfuri",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "nodemon": "^3.1.0",
    "ts-loader": "^9.5.1",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "typescript": "^5.4.3"
  }
}
